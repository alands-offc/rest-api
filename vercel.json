{
  "version": 2,
  "builds": [
    { "src": "index.js", "use": "@vercel/node" }
  ],
"ignoreCommand": "git config core.ignorecase false",
  "routes": [
    { "src": "/(.*)", "dest": "/index.js" }
  ]
}
